{
  "name": "@tomasjs/logging",
  "version": "1.2.0",
  "description": "Support for logging in a TomasJS infrastructure.",
  "author": "viex",
  "license": "ISC",
  "types": "./**/*.d.ts",
  "keywords": [
    "tomas",
    "tomasjs",
    "typescript",
    "viex",
    "logging"
  ],
  "scripts": {
    "compile-clean": "del dist /s /q",
    "compile-build": "tsc -b && tsc-alias",
    "compile": "npm run compile-clean && npm run compile-build && node ./dist/SetupPackage.js",
    "publish": "cd ./dist && npm publish --access public",
    "test": "jest",
    "testw": "jest --watchAll --verbose"
  },
  "dependencies": {
    "@tomasjs/core": "^1.1.0",
    "winston": "^3.8.2"
  },
  "peerDependencies": {
    "@tomasjs/core": "^1.1.0",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "@jest/globals": "^29.5.0",
    "@types/jest": "^29.5.1",
    "jest": "^29.5.0",
    "nodemon": "^2.0.22",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.8.5",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^4.9.4"
  }
}