/**
 * The purpose of this file is for development only.
 *
 * Sometimes the host package will need to import other packages
 * to allow the rest of the source files to use such package.
 *
 * E.g. the "reflect-metadata" package must be imported in order
 * to access the Reflect.decorate method.
 *
 * This file must NOT be included in the final build.
 */

import "reflect-metadata";
import "express-fileupload";
