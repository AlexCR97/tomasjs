{
  "name": "@tomasjs/grpc",
  "version": "1.0.0",
  "description": "Support for gRPC in a TomasJS app.",
  "author": "viex",
  "license": "ISC",
  "types": "./**/*.d.ts",
  "keywords": [
    "tomas",
    "tomasjs",
    "typescript",
    "viex",
    "PACKAGE_NAME"
  ],
  "scripts": {
    "test": "jest",
    "build": "del dist /s /q && tsc -b && tsc-alias && node ./dist/SetupPackage.js",
    "publish": "cd ./dist && npm publish --access public"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.9.3",
    "@grpc/proto-loader": "^0.7.10",
    "google-protobuf": "^3.21.2"
  },
  "peerDependencies": {
    "@grpc/grpc-js": "^1.9.3",
    "@grpc/proto-loader": "^0.7.10",
    "google-protobuf": "^3.21.2"
  },
  "devDependencies": {
    "@jest/globals": "^29.3.1",
    "@types/jest": "^29.2.3",
    "jest": "^29.3.1",
    "nodemon": "^2.0.22",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.8.5",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^4.9.4"
  }
}
